Questo package è necessario per la creazione dei dataset e dei moduli per il funzionamento delle API custom per la
skill Alexa "Chiedi ad UniMe".

Gli script devono essere eseguiti tramite CLI python

--PARAMETRI SCRIPT--

1) -> grade_guess_ds_creator.py {aa_ord_id} {cds_id}

2) -> new_nn_model_regr.py {aa_ord_id} {cds_id} {ad_id}

-----

3) -> subscr_guess_ds_creator.py {cds_id}

4) -> test_regression.py {cds_id} {ad_id}

****IMPORTANTE****

inserire parametri in config.yaml, altrimenti non funziona.